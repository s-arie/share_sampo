<div class='container px-5 px-sm-0'>
  <div class='row'>
    <h2>投稿一覧</h2>
      <% tag_relationships.each do |tag_name|%>
        <p><%= tag_name.postimage %></p>
      <% end %>
    </div>
  </div>
</div>
